# Screenshots Needed

- [x] 2026-09-10 simlocal ADAMANDLINDS: checkout proof image added 2026-09-11 (`src/assets/simlocal/ADAMANDLINDS.png`), showing the 10% discount applied. Proof images for all four other SimLocal codes (EMMANUEL, LUKEPALMS, SAVANNAH, TWOROAMING) were added at the same time; owner used these to confirm those four are actually 5% off, not 10% as previously listed. No UK-number proof image yet; still fine to add later if the owner wants it.
- [ ] 2026-09-12 instabridge SP20 and TRAVELESIMEXPERT: owner personally tested both on Instabridge's web store checkout (store.instabridge.com/mobile-data) and confirmed both apply the 20% discount, but the codes popped up as applied/invalid faster than a screenshot could be taken, so there's no proof image yet. Owner also confirmed DPF10 (the former primary code) is now invalid at checkout, same issue with no screenshot. If the owner retests later, save proof images to `src/assets/instabridge/SP20.png`, `src/assets/instabridge/TRAVELESIMEXPERT.png`, and `src/assets/instabridge/DPF10.png` (the last showing the invalid/rejected state) and the article/data are already written to use them once present.
- [x] 2026-09-15 airhub ESIMOFFER5: owner personally tested at Airhub's checkout and uploaded proof (`src/assets/airhub/ESIMOFFER5.png`, showing 5% off applied in the order summary). Also fixed a data bug where this and three other public codes (MARCHFOREGYPT, NAMERICA4, JPESIM5) were stored with a stray "COPY" suffix left over from how they were originally sourced.
- [ ] 2026-09-15 airhub ADAMANDLINDS: owner personally tested at Airhub's checkout and confirmed our own code is not currently applying any discount (same for 2025, SUMMER2025, MARCHFOREGYPT, NAMERICA4, and JPESIM5, all now marked invalid). Owner has contacted Airhub directly to get it fixed and believes this is temporary, so it's still listed as our primary code rather than being replaced. Once Airhub confirms a fix, re-test and save a proof image to `src/assets/airhub/ADAMANDLINDS.png`, then flip `isInvalid` back to false in codes.json and restore it as the featured code in airhub.njk and the airhub-vs-instabridge comparison.
- [ ] 2026-09-19 esimcardus ESIM10: no checkout proof image exists yet (`src/assets/esimcardus/ESIM10.png`). The code is confirmed advertised directly on eSIM Card's own homepage (verified again this run via a live fetch), but nobody has actually run it through checkout and screenshotted the discount applied. Save one to that path whenever the owner tests it, the same way Holafly/SimLocal/Airhub have proof images.
- [ ] 2026-09-20 nomad MEMORY20: new code found this run, advertised directly on Nomad's own homepage (20% off plans 10GB+), added to codes.json but never run through checkout. No proof image at `src/assets/nomad/MEMORY20.png`. Test at checkout and save one there before treating it as fully confirmed.
- [ ] 2026-09-20 airhub AIRHUBAPP05: new code found this run, advertised on Airhub's own homepage as "App exclusive 5% off at checkout" (mobile app only). Added to codes.json but never tested, since it requires ordering through the Airhub app rather than the web checkout we can reach. No proof image at `src/assets/airhub/AIRHUBAPP05.png`. Test via the app and save one there before treating it as fully confirmed.
- [ ] 2026-09-21 alosim ALOSAVE: no checkout proof image exists yet (`src/assets/alosim/ALOSAVE.png`). The code is used prominently in the new eSIM Card vs AloSIM comparison page added this run; it's the best public code we've verified (advertised discount matches AloSIM's own site), but nobody has actually run it through checkout and screenshotted the discount applied. Save one to that path whenever the owner tests it, same as the other proof images.
