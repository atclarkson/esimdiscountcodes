---
layout: base.njk
title: "eSIM Discount Codes - Best Promo Codes for International Travel"
description: "Find verified discount and promo codes for top eSIM providers. Save on Holafly, Airalo, Saily and more. Updated daily with working promo codes for international travel."
---

<div class="home-page">
    <!-- Hero Section -->
    <section class="hero">
        <h1>Find the Best eSIM Promo & Discount Codes</h1>
        <p class="hero-subtitle">Save money on international eSIM plans with verified promo codes</p>
        {% set totalCodes = 0 %}
        {% set totalProviders = 0 %}
        {% set maxDiscount = 0 %}
        {% set bestCode = null %}
        {% set bestProvider = null %}
        
        {% for key, provider in codes %}
            {% if provider.codes and provider.codes.length > 0 %}
                {% set totalProviders = totalProviders + 1 %}
                {% set totalCodes = totalCodes + provider.codes.length %}
                
                {% for code in provider.codes %}
                    {% set discountNum = code.discount | replace('%', '') | replace('OFF', '') | replace('SPECIAL', '10') | replace('EXTRA', '10') | int %}
                    {% if discountNum > maxDiscount %}
                        {% set maxDiscount = discountNum %}
                        {% set bestCode = code.code %}
                        {% set bestProvider = provider.name %}
                    {% endif %}
                {% endfor %}
            {% endif %}
        {% endfor %}
        
        {% for key, provider in codes %}
            {% if provider.codes and provider.codes.length > 0 %}
                {% set totalProviders = totalProviders + 1 %}
                {% set totalCodes = totalCodes + provider.codes.length %}
                
                {% for code in provider.codes %}
                    {% set discountNum = code.discount | replace('%', '') | replace('OFF', '') | replace('SPECIAL', '10') | replace('EXTRA', '10') | int %}
                    {% if discountNum > maxDiscount %}
                        {% set maxDiscount = discountNum %}
                        {% set bestCode = code.code %}
                        {% set bestProvider = provider.name %}
                    {% endif %}
                {% endfor %}
            {% endif %}
        {% endfor %}
        
        {% for key, provider in codes %}
            {% if provider.codes and provider.codes.length > 0 %}
                {% set totalProviders = totalProviders + 1 %}
                {% set totalCodes = totalCodes + provider.codes.length %}
                
                {% for code in provider.codes %}
                    {% set discountNum = code.discount | replace('%', '') | replace('OFF', '') | replace('SPECIAL', '10') | replace('EXTRA', '10') | int %}
                    {% if discountNum > maxDiscount %}
                        {% set maxDiscount = discountNum %}
                        {% set bestCode = provider.codes.code %}
                    {% endif %}
                {% endfor %}
            {% endif %}
        {% endfor %}
        
        <div class="hero-stats">
            <span>✅ {{ totalCodes }}+ verified codes</span>
            <span>🌍 {{ totalProviders }} top providers</span>
            <span>💰 Save up to {{ maxDiscount }}%</span>
        </div>
    </section>

    <!-- What is eSIM Section -->
    <section class="what-is-esim">
        <h2>What is an eSIM?</h2>
        <p>An eSIM (embedded SIM) is a digital SIM card that lets you connect to mobile networks without a physical SIM card. Perfect for international travel, eSIMs eliminate expensive roaming charges and give you instant connectivity in 170+ countries.</p>
        
        <div class="esim-benefits">
            <div class="benefit">
              <div style="text-align:center;">
                <span class="fa-stack fa-2x">
                  <i class="fa fa-sim-card fa-stack-1x" aria-hidden="true"></i>
                  <i class="fa fa-ban fa-stack-2x" style="color:Tomato" aria-hidden="true"></i>
                </span>
              </div>
              <h3>No Physical SIM</h3>
              <p>Digital activation via QR code</p>
            </div>
            <div class="benefit">
              <div style="text-align:center;">
                <span class="fa-stack fa-2x" >
                  <i class="fa-solid fa-square fa-stack-2x" style="color:LimeGreen" aria-hidden="true"></i>
                  <i class="fa-solid fa-globe fa-stack-1x fa-inverse" aria-hidden="true"></i>
                </span>
              </div>
              <h3>Global Coverage</h3>
              <p>Works in 170+ countries worldwide</p>
            </div>
            <div class="benefit">
              <div style="text-align:center;">
                <span class="fa-stack fa-2x">
                  <i class="fa-solid fa-square fa-stack-2x" style="color:LimeGreen" aria-hidden="true"></i>
                  <i class="fa-solid fa-dollar-sign fa-stack-1x fa-inverse" aria-hidden="true"></i>
                </span>
              </div>
              <h3>Cost Effective</h3>
              <p>Much cheaper than roaming charges</p>
            </div>
            </div>
        </div>
    </section>

    <!-- Providers Grid -->
    <section class="providers-section">
        <h2>Top eSIM Providers with Discount Codes</h2>
        <div class="providers-grid">
            {% for key, provider in codes %}
            <a href="/{{ key }}/" class="provider-card-link">
                <div class="provider-card">
                    <div class="provider-logo">
                        {% if provider.logo %}
                            <img src="{{ provider.logo }}" alt="{{ provider.name }} logo" class="logo" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                            <div class="logo-fallback" style="display: none;">{{ provider.name }}</div>
                        {% else %}
                            <div class="logo-fallback">{{ provider.name }}</div>
                        {% endif %}
                    </div>
                    <h3>{{ provider.name }} Discount Codes</h3>
                    {% if provider.primaryCode %}
                    <p class="discount">Save with code <strong>{{ provider.primaryCode }}</strong></p>
                    {% else %}
                    <p class="discount">Coming soon</p>
                    {% endif %}
                    <p>{{ provider.description }}</p>
                </div>
            </a>
            {% endfor %}
        </div>
    </section>

    
    <section class="cta-section">
        <h2>Ready to Save?</h2>
        <p>Start using eSIMs today and enjoy hassle-free international connectivity.</p>
        <a href="#compare" class="cta-button">Browse All Discount Codes</a>
    </section>

    <section class="best-code-section">
      <h2>Best Discount Code</h2>
        <div class="best-code-card">
            <p>Use code <strong>{{ bestCode }}</strong> for <strong>{{ maxDiscount }}% off</strong> at <strong>{{ bestProvider }}</strong> — our top verified discount!</p>
        </div>
    </section>

    <div id="compare" class="comparison-table-container">
      <h2>Compare eSIM Providers</h2>
      <table class="comparison-table">
        <thead>
          <tr>
            <th></th>
            <th></th>
            <th>Best Discount</th>
            <th>Rating</th>
            <th>Global Packages <i class="fa fa-globe"></i></th>
            <th>SMS Plans <i class="fa fa-mobile-alt"></i></th>
            <th>Unlimited Data Plans <i class="fa fa-bolt"></i></th>
          </tr>
        </thead>
        <tbody>
          {% for key, provider in codes %}
          <tr>
            <td>
                <a href="/{{ key }}/" class="provider-link-btn" style="display:inline-block;padding:6px 14px;background:#007bff;color:#fff;border-radius:4px;text-decoration:none;font-weight:500;transition:background 0.2s;width:104px;text-align:center;"
                   onmouseover="this.style.background='#0056b3';"
                   onmouseout="this.style.background='#007bff';">
                {{ provider.name }}
                </a>
            </td>
            <td>
              <div class="provider-logo">
                {% if provider.logo %}
                  <img src="{{ provider.logo }}" alt="{{ provider.name }} logo" class="logo-img" onerror="this.style.display='none'; this.nextElementSibling.style.display='block';">
                  <span class="logo-fallback" style="display:none;">{{ provider.name }}</span>
                {% else %}
                  <span class="logo-fallback">{{ provider.name }}</span>
                {% endif %}
              </div>
            </td>
            <td>
                {% if provider.codes[0].code and provider.codes[0].discount %}
                <span><strong>{{ provider.codes[0].discount }}</strong> <span style="color:#28a745;">({{ provider.codes[0].code }})</span></span>
                {% else %}
                <span>—</span>
                {% endif %}
            </td>
            <td>
              {% if provider.totalScore %}
                <span class="star-rating">
                  {% set score = provider.totalScore | int %}
                  {% for i in range(1, 6) %}
                    {% if i <= score %}
                      <i class="fa fa-star" style="color: #ffc107;"></i>
                    {% else %}
                      <i class="fa fa-star" style="color: #e4e5e9;"></i>
                    {% endif %}
                  {% endfor %}
                  <span class="score-label">{{ score }}/5</span>
                </span>
              {% else %}
                <span>—</span>
              {% endif %}
            </td>
            <td>
              {% if provider.globalRegionalPackage %}
                <i class="fa fa-check"></i>
              {% else %}
                <i class="fa fa-times"></i>
              {% endif %}
            </td>
            <td>
              {% if provider.smsIncluded %}
                <i class="fa fa-check"></i>
              {% else %}
                <i class="fa fa-times"></i>
              {% endif %}
            </td>
            <td>
              {% if provider.unlimitedData %}
                <i class="fa fa-check"></i>
              {% else %}
                <i class="fa fa-times"></i>
              {% endif %}
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>

</div>